<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pinecone.hydra.file.ibatis.SymbolicMapper">

    <insert id="insert">
        INSERT INTO `hydra_uofs_symbolic`
            (`guid`, `create_time`, `update_time`, `name`, `symbolic_type`, `reparsed_point`, `target_scheme`, `ext_config`)
        VALUES
            (#{guid}, #{createTime}, #{updateTime}, #{name}, 'INTERNAL', #{reparsedPoint}, 'UOFS', #{extConfig})
    </insert>

    <delete id="remove">
        DELETE FROM `hydra_uofs_symbolic` WHERE `guid` = #{guid}
    </delete>

    <select id="getSymbolicByGuid" resultType="com.pinecone.hydra.storage.file.entity.GenericSymbolic">
        SELECT `id` AS enumId, `guid`, `create_time` AS createTime, `update_time` AS updateTime, `name`,
               `reparsed_point` AS reparsedPoint, `target_scheme` AS targetScheme, `ext_config` AS extConfig
        FROM `hydra_uofs_symbolic`
        WHERE `guid` = #{guid} AND `symbolic_type` = 'INTERNAL'
    </select>

    <select id="getSymbolicByNameGuid" resultType="com.pinecone.hydra.storage.file.entity.GenericSymbolic">
        SELECT `id` AS enumId, `guid`, `create_time` AS createTime, `update_time` AS updateTime, `name`,
               `reparsed_point` AS reparsedPoint, `target_scheme` AS targetScheme, `ext_config` AS extConfig
        FROM `hydra_uofs_symbolic`
        WHERE `guid` = #{guid} AND `name` = #{nodeName} AND `symbolic_type` = 'INTERNAL'
    </select>

    <select id="isSymbolicMatchedByNameGuid" resultType="boolean">
        SELECT COUNT(*) FROM `hydra_uofs_symbolic`
        WHERE `guid` = #{guid} AND `name` = #{nodeName} AND `symbolic_type` = 'INTERNAL'
    </select>

    <sql id="InternalSymbolicFilter">
        <where>
            `symbolic_type` = 'INTERNAL'
            <if test="keyword != null and keyword != ''">
                AND (
                    `name` LIKE CONCAT('%', #{keyword}, '%')
                    OR `reparsed_point` LIKE CONCAT('%', #{keyword}, '%')
                    OR CAST(`guid` AS CHAR) LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
        </where>
    </sql>

    <select id="listPage" resultType="com.pinecone.hydra.storage.file.entity.GenericSymbolic">
        SELECT `id` AS enumId, `guid`, `create_time` AS createTime, `update_time` AS updateTime, `name`,
               `reparsed_point` AS reparsedPoint, `target_scheme` AS targetScheme, `ext_config` AS extConfig
        FROM `hydra_uofs_symbolic`
        <include refid="InternalSymbolicFilter"/>
        ORDER BY `id` DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*)
        FROM `hydra_uofs_symbolic`
        <include refid="InternalSymbolicFilter"/>
    </select>

    <select id="getGuidsByName" resultType="com.pinecone.framework.util.id.GUID">
        SELECT `guid` FROM `hydra_uofs_symbolic` WHERE `name` = #{name} AND `symbolic_type` = 'INTERNAL'
    </select>

    <select id="getGuidsByNameID" resultType="com.pinecone.framework.util.id.GUID">
        SELECT `guid` FROM `hydra_uofs_symbolic`
        WHERE `name` = #{name} AND `guid` = #{guid} AND `symbolic_type` = 'INTERNAL'
    </select>

</mapper>
