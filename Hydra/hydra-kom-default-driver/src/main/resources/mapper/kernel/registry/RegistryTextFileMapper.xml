<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pinecone.hydra.registry.ibatis.RegistryTextFileMapper">

    <insert id="insert">
        INSERT INTO `hydra_registry_conf_node_text_value`
        (
            `guid`,
            `value`,
            `create_time`,
            `update_time`,
            `type`
        )
        VALUES
        (
            #{guid},
            #{value},
            #{createTime},
            #{updateTime},
            #{type}
        )
    </insert>

    <delete id="remove">
        DELETE FROM `hydra_registry_conf_node_text_value`
        WHERE `guid` = #{guid}
    </delete>

    <select id="getTextValue" resultType="com.pinecone.hydra.registry.entity.GenericTextValue">
        SELECT
            `id` AS `enumId`,
            `guid`,
            `value`,
            `create_time` AS `createTime`,
            `update_time` AS `updateTime`,
            `type`
        FROM `hydra_registry_conf_node_text_value`
        WHERE `guid` = #{guid}
    </select>

    <update id="update">
        UPDATE `hydra_registry_conf_node_text_value`
        SET
            `value` = #{value},
            `update_time` = #{updateTime},
            `type` = #{type}
        WHERE `guid` = #{guid}
    </update>

    <insert id="copyTextValueTo">
        INSERT INTO `hydra_registry_conf_node_text_value`
        (
            `guid`,
            `type`,
            `create_time`,
            `update_time`,
            `value`
        )
        SELECT
            #{destinationGuid},
            `type`,
            `create_time`,
            `update_time`,
            `value`
        FROM `hydra_registry_conf_node_text_value`
        WHERE `guid` = #{sourceGuid}
        AND `guid` != #{destinationGuid}
    </insert>

</mapper>
