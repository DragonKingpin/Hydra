# Hydra Technical & Construction Minimum Constitution

Hydra 体系是一个高达 50 ~ 100万行级当量的巨型项目，拥有上万个文件，
因此 Topology 和 Architecture 需要严格正确化，本文是最小开发宪法。

## 技术导向
1. Hydra 面向底层，结构、架构优先。
2. 原则上严禁 MVP 思想，应用开发模式不适用于 Hydra 体系。
3. 严格面向对象、严格一致性、严格稳定性。
4. 保持方案和施工的优雅，快准狠且优雅。!important

## 贯彻落实
### 阶级分明，不可越级
1. 金字塔 Hierarchy 不可撼动、不可偷懒、不可捷径，越级会直接报错，不要去试了。 
   比如 Spring 都是隔离的，越级扫包会报错（比如应用层直接访问 Hydra 的 Mapper）。
2. 目录架构就是 SPEC，就是 Hierarchy 结构，注意包隔离，目录结构是系统命名法的体现（最为严肃，关系整个项目的最短路径寻址）。
3. Hydra 底层 Mapper、Entity、算法、设计等 只能放 Hydra、Lord（如Odin、Redqueen、Skynet、Titan等） 
等底层领域内。
4. 底层、中间层、最高应用层，都是递归 Hierarchy，都不可越级，实践中注意崽崽等级（尤其是中间层崽崽）的目录层级和包隔离情况。
5. 每一层级一定有代理中央，不要直接越级用底层原子能力，找各级中央要功能（常见格：Instrument、Manager、Orchestrator、Regiment、Service 等）。
   参见 `com.pinecone.framework.system.regime.*`。
6. 常见越级行为：4.1 Controller 里面不要写 Service 逻辑；4.1 应用层 Service 里面不要写 底层逻辑；

### 复用、复利、最小职责
1. 原则上贯彻落实复用、复利第一性，优先改造和利用已有功能，不要乱建文件、乱建功能（不要贪心，如果明确必要新建也行）。
2. 原则上贯彻落实最小职责、里氏替换等基本架构要求。怎么做？（说人话：该拆文件就拆、该写类写类，但是不要过分）

### 常见数理、计算机底层实践最低要求
1. 结构工程：最坏 log 级寻址。怎么做：清晰、隔离、结构化、树化、文件拆分、类拆分。
2. 项目文件、代码结构必须树化，2 ^ 4 (最多 16 个文件每文件夹或某结构内的对象数)
3. 熵增控制：写接口、接口隔离、文件隔离、继承关系清晰。怎么做：命名即控制，文件名就是索引，代码就是索引。
4. 历史代码原则上就应该是优秀作文 SPEC，以大范围重复的风格为准 (P > 70%)，偶尔几个屎山害群之马，不要学坏参考。
5. Sequential、偏序关系 非常严格，注意施工方案序。怎么做：乱序执行无所谓，但是功能 Topo 顺序不要混一坨。
6. 近邻性、中央化。怎么做：功能聚集化、聚类化，相似的单元尽可能空间距离最短（如文件名、功能近邻的整整齐齐聚一起）。
7. 严打贪心：贪心很容易物极必反，不要过分追求任何一个子项。说人话：没有要你搞单维度凸优化，而是全局最优捏。
   再说人话：比如树化，不要极端，通常是多个要求叠加，不要屁大点功能也树化（还有聚集性约束呢）。