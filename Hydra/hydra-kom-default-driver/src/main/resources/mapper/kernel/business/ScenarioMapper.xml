<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pinecone.hydra.business.ibatis.ScenarioMapper">

    <resultMap id="ScenarioMap" type="com.pinecone.hydra.business.entity.GenericScenarioElement">
        <id column="id" property="enumId"/>
        <result column="guid" property="guid"/>
        <result column="scenario_code" property="scenarioCode"/>
        <result column="scenario_type" property="scenarioType"/>
        <result column="priority" property="priority"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO hydra_business_scenario_node (
            guid, scenario_code, scenario_type, priority, create_time, update_time
        )
        VALUES (
            #{guid}, #{scenarioCode}, #{scenarioType}, #{priority}, #{createTime}, #{updateTime}
        )
    </insert>

    <delete id="remove">
        DELETE FROM hydra_business_scenario_node WHERE guid = #{guid}
    </delete>

    <select id="get" resultMap="ScenarioMap">
        SELECT id, guid, scenario_code, scenario_type, priority, create_time, update_time
        FROM hydra_business_scenario_node
        WHERE guid = #{guid}
    </select>

    <update id="update">
        UPDATE hydra_business_scenario_node
        SET
            scenario_code = #{scenarioCode},
            scenario_type = #{scenarioType},
            priority      = #{priority},
            update_time   = #{updateTime}
        WHERE guid = #{guid}
    </update>

    <select id="queryByScenarioCode" resultMap="ScenarioMap">
        SELECT id, guid, scenario_code, scenario_type, priority, create_time, update_time
        FROM hydra_business_scenario_node
        WHERE scenario_code = #{scenarioCode}
    </select>

</mapper>
