<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.walnut.odin.task.mapper.InstanceLineageAdjacentMapper">

    <insert id="insert" parameterType="com.walnut.odin.conduct.entity.GenericInstanceLineageAdjacent">
        INSERT INTO odin_atlas_task_instance_lineage_adjacent (
            guid,
            parent_guid
        ) VALUES (
                     #{guid},
                     #{parentGuid}
                 )
    </insert>

    <select id="countByGuidAndParentGuid" resultType="long">
        SELECT COUNT(*)
        FROM odin_atlas_task_instance_lineage_adjacent
        WHERE guid = #{guid}
          AND parent_guid = #{parentGuid}
    </select>

</mapper>
