package com.walnut.odin.processor.event;

public enum TaskProcessorEventType {
    JoinRequested,
    JoinAccepted,
    JoinRejected,
    Registered,
    Detached,
    Unregistered,
    MetadataUpdated
}
