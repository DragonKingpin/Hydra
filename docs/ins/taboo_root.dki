@scope(在没有明确说明要写测试文件 || 除非显式给出要求)
#else，请不要写测试文件。

@scope(在没有明确说明需要测试前 || 除非显式给出要求)
#else，请不要执行测试。

@warning(Hydra 是一个非常巨大的项目（高达4000+的文件），请勿非计划内地执行大规模目录扫描)
@taboo(一次性读入超过50个文件以上)
@scope(
    (在没有明确放行前 && 某目录文件数超过 > 20) || 除非显式给出要求 || 显式的全局任务 ||
    @privilege_granted(如果有明确必要，可以向用户发起权限申请)
)
#else，请不要全量读某个包下的文件，最大读入不要超过20个，超过时需要向用户确认一下。

@scope(Windows环境下)
强制使用 utf8 编码读取文本文件内容。

@scope(你需要改写文档文件时)
对于 `<!-- impregnable --> 我是内容 <!-- impregnable /-->` 括起来的内容，不可撼动、不可修改。
同时这个文法你也可以用。