<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pinecone.hydra.business.ibatis.TreeMapper">

    <resultMap id="TreeMap" type="com.pinecone.hydra.business.entity.GenericNodeTree">
        <id column="id" property="enumId"/>
        <result column="guid" property="guid"/>
        <result column="parent_guid" property="parentGuid"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO hydra_business_node_tree ( guid, parent_guid, create_time, update_time )
        VALUES ( #{guid}, #{parentGuid}, #{createTime}, #{updateTime} )
    </insert>

    <insert id="insertOwned">
        INSERT INTO hydra_business_node_tree ( guid, parent_guid )
        VALUES ( #{guid}, #{parentGuid} )
    </insert>

    <delete id="remove">
        DELETE FROM hydra_business_node_tree WHERE guid = #{guid}
    </delete>

    <select id="get" resultMap="TreeMap">
        SELECT id, guid, parent_guid, create_time, update_time
        FROM hydra_business_node_tree
        WHERE guid = #{guid}
        LIMIT 1
    </select>

    <select id="fetchParentGuid" resultType="com.pinecone.framework.util.id.GUID">
        SELECT parent_guid
        FROM hydra_business_node_tree
        WHERE guid = #{guid}
        LIMIT 1
    </select>

    <select id="fetchChildrenGuids" resultType="com.pinecone.framework.util.id.GUID">
        SELECT guid
        FROM hydra_business_node_tree
        <where>
            <if test="parentGuid != null">
                parent_guid = #{parentGuid}
            </if>
            <if test="parentGuid == null">
                parent_guid IS NULL
            </if>
        </where>
        ORDER BY id
    </select>

    <select id="fetchRootGuids" resultType="com.pinecone.framework.util.id.GUID">
        SELECT guid
        FROM hydra_business_node_tree
        WHERE parent_guid IS NULL
        ORDER BY id
    </select>

    <update id="updateParentGuid">
        UPDATE hydra_business_node_tree
        SET parent_guid = #{parentGuid}
        WHERE guid = #{targetGuid}
    </update>

    <delete id="removeTreeNodeByParentGuid">
        DELETE FROM hydra_business_node_tree WHERE parent_guid = #{parentGuid}
    </delete>

    <select id="countNode" resultType="long">
        SELECT COUNT(*)
        FROM hydra_business_node_tree
        WHERE guid = #{guid}
        <if test="parentGuid != null">
            AND parent_guid = #{parentGuid}
        </if>
        <if test="parentGuid == null">
            AND parent_guid IS NULL
        </if>
    </select>

</mapper>
